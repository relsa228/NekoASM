import "./ui/stop-btn.css";
import { Button } from "~/components/ui/button";

const StopBtn = () => {
  return (
    <Button class="stop-btn">
      <h1>[||]</h1>
    </Button>
  );
};

export default StopBtn;
